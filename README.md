# The most basic Node.js + Express setup with use of ESModules.

Only few basic folders, server, eslint, prettier, dotenv, nodemon, ejs and '/' route to check if app is starting.

# How to use

npm i => npm run start => done
